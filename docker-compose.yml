services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/data:/app/app/data   # persist chroma + sqlite across restarts
    restart: unless-stopped
